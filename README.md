# NLP-Project
The model we will be using is the roBERTA model. The dataset will be in the Xhosa language. The language was chosen because we noticed that the model does not perform as well in deciphering Xhosa texts, compared to the other languages it has been pretrained on.
